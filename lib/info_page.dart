import 'package:flutter/material.dart';

class TylenolPage extends StatelessWidget {
  const TylenolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('정보'),
      ),
      body: const Center(
        child: Text('타이레놀에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class ColdaeWonPage extends StatelessWidget {
  const ColdaeWonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('콜대원 정보'),
      ),
      body: const Center(
        child: Text('콜대원에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class PancolPage extends StatelessWidget {
  const PancolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('판콜 정보'),
      ),
      body: const Center(
        child: Text('판콜에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class SohwajePage extends StatelessWidget {
  const SohwajePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('소화제 정보'),
      ),
      body: const Center(
        child: Text('소화제에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class WiyeomPage extends StatelessWidget {
  const WiyeomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('위염약 정보'),
      ),
      body: const Center(
        child: Text('위염약에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class ByeonbiPage extends StatelessWidget {
  const ByeonbiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('변비약 정보'),
      ),
      body: const Center(
        child: Text('변비에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class HongsamPage extends StatelessWidget {
  const HongsamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('홍삼 정보'),
      ),
      body: const Center(
        child: Text('홍삼에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class LemonaPage extends StatelessWidget {
  const LemonaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('레모나 정보'),
      ),
      body: const Center(
        child: Text('레모나에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class CheolbunjePage extends StatelessWidget {
  const CheolbunjePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('철분제 정보'),
      ),
      body: const Center(
        child: Text('철분제에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class MadekasolPage extends StatelessWidget {
  const MadekasolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('마데카솔 정보'),
      ),
      body: const Center(
        child: Text('마데카솔에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class HusidinPage extends StatelessWidget {
  const HusidinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('후시딘 정보'),
      ),
      body: const Center(
        child: Text('후시딘에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class HwasangPage extends StatelessWidget {
  const HwasangPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('화상연고 정보'),
      ),
      body: const Center(
        child: Text('화상연고에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class IngongnunmulPage extends StatelessWidget {
  const IngongnunmulPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('인공눈물 정보'),
      ),
      body: const Center(
        child: Text('인공눈물에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}

class SumyeonjePage extends StatelessWidget {
  const SumyeonjePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('수면제 정보'),
      ),
      body: const Center(
        child: Text('수면제에 대한 정보가 여기에 표시됩니다.'),
      ),
    );
  }
}
